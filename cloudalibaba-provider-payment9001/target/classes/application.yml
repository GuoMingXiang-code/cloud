server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: 10.7.31.29:1111   #nginx的端口，连接nacos集群
#        server-addr: 127.0.0.1:8848


management:
  endpoints:
    web:
      exposure:
        include: "*"
